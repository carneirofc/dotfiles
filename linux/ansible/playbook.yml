---
- hosts: all
  become: false
  gather_facts: true
# connection: local

  vars:
    setup_zsh: true
    setup_fonts: false
    setup_nvim: false
    setup_alacritty: true
    setup_kitty: true
    setup_fastfetch: true

  roles:
    - setup-workstation
